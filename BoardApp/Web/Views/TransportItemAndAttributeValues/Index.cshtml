@model IEnumerable<Domain.TransportItemAndAttributeValue>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TransportItem.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TransportItemTypeAttributeValue.Value)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TransportItem.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TransportItemTypeAttributeValue.Value)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TransportItemAndAttributeValueId }) |
            @Html.ActionLink("Details", "Details", new { id=item.TransportItemAndAttributeValueId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TransportItemAndAttributeValueId })
        </td>
    </tr>
}

</table>
